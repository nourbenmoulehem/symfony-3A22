{% extends 'base.html.twig' %}

{% block title %}Modifier un livre{% endblock %}

{% block content %}
  <div class="col-md-8">
    {{ form_start(form) }}
      {{ form_widget(form) }}
      <button type="submit" class="btn btn-primary mt-3">Modifier Livre</button>
    {{ form_end(form) }}
  </div>
{% endblock %}
